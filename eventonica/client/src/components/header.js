import calendar from "../halloween.png";

const Header = () => {
return (
    <header>
        <img src={calendar} alt="Calendar Halloween Logo" />
        <h1>Eventonica</h1>
    </header>

);

};

export default Header;